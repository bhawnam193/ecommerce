export const API = `${window.location.protocol}//${window.location.hostname}:8000/api`; 

export const Admin = `${window.location.protocol}//${window.location.hostname}:8000`; 

//process.env.REACT_APP_API_URL;